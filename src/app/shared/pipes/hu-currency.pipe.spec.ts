import { HuCurrencyPipe } from './hu-currency.pipe';

describe('HuCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new HuCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
